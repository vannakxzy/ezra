import 'package:flutter/material.dart';

class SecurityLoginWidget extends StatelessWidget {
  const SecurityLoginWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: const Text('SecurityLogin Widget'),),
      body: const Placeholder(),
    );
  }
}