import 'package:flutter/material.dart';

class CommentInQuestionWidget extends StatelessWidget {
  const CommentInQuestionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: const Text('CommentInQuestion Widget'),),
      body: const Placeholder(),
    );
  }
}