import 'package:flutter/material.dart';

class CommentInAnswerWidget extends StatelessWidget {
  const CommentInAnswerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: const Text('CommentInAnswer Widget'),),
      body: const Placeholder(),
    );
  }
}