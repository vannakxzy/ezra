import 'dart:io';

import 'package:mason/mason.dart';

void run(HookContext context) async {
  // final runFolder = 'lib/features/${context.vars['feature']}/presentation/bloc';
  // final progress = context.logger.progress('Generating Freezed (Bloc)');

  // Run `flutter packages get` after generation.
  // await Process.run('flutter', ['pub', 'get']);

  // await Process.run(
  //   'dart',
  //   [
  //     'run',
  //     'build_runner',
  //     'build',
  //     '-d',
  //   ],
  // );
  // progress.complete();
}


// dart run build_runner build --release --build-filter=lib/src/usecase/authenticate_bic_device_usecase.freezed.dart